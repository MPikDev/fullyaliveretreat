# Core framework
Django>=5.2,<5.3

# WSGI server + static file serving
gunicorn>=23.0.0
whitenoise[brotli]>=6.9.0

# Database
dj-database-url>=2.3.0
psycopg[binary]>=3.2.0

# Payments
django-paypal>=2.1

# Auth hardening (login throttling / lockout)
django-axes>=7.0.0

# Email
yagmail>=0.15.293

# Configuration
python-dotenv>=1.0.1

# HTTP stack (transitively required; pinned to patched versions)
requests>=2.32.4
urllib3>=2.2.2
certifi>=2024.7.4
idna>=3.7
